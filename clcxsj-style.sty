\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{clcxsj-style}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%调用的宏包
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% 基本工具宏包
% 
\RequirePackage{amsmath,amsthm}
\RequirePackage{hologo,ulem,verbatim,fancyvrb,layout,multicol}
\RequirePackage{colortbl,tabularx,multirow,makecell,booktabs}
\RequirePackage{graphicx,xcolor,float,subfig}
\RequirePackage{makeidx}
\RequirePackage[inner=1in,outer=1.5in,vmargin=1in,marginparwidth=1in]{geometry}
\RequirePackage{tikz}


%%设置字体：借助宏包 fontspec 可选参数[no-math]
%%\setmainfont[<可选参数>]{<字体名>} %正文罗马字体族
%%\setsansfont[<可选参数>]{<字体名>} %无衬线字体族
%%\setmonofont[<可选参数>]{<字体名>} %打印机字体族
%%\newfontfamily<命令>[]{} %设置新的字体族命令
%%在CTeX套件中对应设置项为：
%%\setCJKmainfont[<可选参数>]{<字体名>} %正文罗马字体族
%%\setCJKsansfont[<可选参数>]{<字体名>} %无衬线字体族
%%\setCJKmonofont[<可选参数>]{<字体名>} %打印机字体族
%%\setCJKfamilyfont{<中文字体族名称>}[]{<字体名>}
%%如下例：\setCJKmainfont[BoldFont=SimHei,ItalicFont=KaiTi,BoldItalicFont=LiShu]{SimSun}
%%\setCJKmainfont[BoldFont=SimHei,ItalicFont=KaiTi,BoldItalicFont=LiShu]{SimSun}

%
% 字体和符号宏包
% 
\RequirePackage{lmodern}  % 西文正文字体使用 Latin Modern
\RequirePackage{amssymb,latexsym,textcomp,mathrsfs,euscript,yhmath} % 与默认字体不冲突的一些符号包
\RequirePackage[LGR,T1]{fontenc}
\RequirePackage{listings} %%专用于代码排版的LaTeX宏包， 可对关键字、注释、字符串等
                          %%使用不同的字体和颜色，也可为代码添加边框、北京背景等风格

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\small\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=4,  %%用于代码中特殊符号的显示
    escapeinside={``}
}
 
\lstset{style=mystyle}


\endinput